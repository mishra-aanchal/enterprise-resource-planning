$kind: http-request
description: Update an inventory item. Requires 'warehouse_manager' or 'admin' role.
url: "{{baseUrl}}/api/inventory/items/:id"
method: PUT
headers:
  - key: Content-Type
    value: application/json
pathVariables:
  id: item-123
body:
  type: json
  content: |-
    {
      "unitPrice": 175,
      "reorderPoint": 25,
      "location": "Warehouse B, Aisle 5"
    }
order: 5000
