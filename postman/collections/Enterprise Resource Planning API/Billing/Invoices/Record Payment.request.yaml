$kind: http-request
description: Record a payment for an invoice. Requires 'billing_manager' or 'admin' role.
url: '{{baseUrl}}/api/billing/invoices/:id/payments'
method: POST
headers:
  - key: Content-Type
    value: application/json
pathVariables:
  id: inv-123
body:
  type: json
  content: |-
    {
      "amount": 6480
    }
order: 5000
